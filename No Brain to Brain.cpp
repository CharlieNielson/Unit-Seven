#include <iostream>
using namespace std;

//int has less storage than double.
// cout << &variable; gives us the variable address.
//pointers have a value of a memory address, and are defined by a *.

//const int * ptr; forbids changing of the int.
//int * const ptr; //constant memory location.
//const int * const ptr;//constant both.

//arrays are automaticaly a pointer.
//arrays are constant, but pointers are not.

int main() {

    //_

    return 0;
}
